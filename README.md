# Benchmarking

A server is needed to access the benchmarking page. This can be accieved using for instance pythons built-in http-server.

```zsh
python3 -m http.server 8080
```
The server should be started from the project root to be able to serve all needed files.

Navigate to http://localhost:8080/bench/javascript/ or http://localhost:8080/bench/wasm/

## JS

## WASM

# Datasets

All datasets are generated by `datagen.c` using a fixed seed (`10`) for reproducibility.
Run `./generate_datasets` from inside the `datasets/` folder to regenerate.

```
gcc -O2 -o generate_datasets datagen.c && ./generate_datasets
```

## Sorting

Used by Mergesort and Quicksort.

| Name        | File                     | Elements   | Value range       | File size |
|-------------|--------------------------|------------|-------------------|-----------|
| Small       | `sorting/small.bin`      | 1 000      | 0 – 2 147 483 647 | ~3.9 KB   |
| Medium      | `sorting/medium.bin`     | 100 000    | 0 – 2 147 483 647 | ~391 KB   |
| Large       | `sorting/large.bin`      | 1 000 000  | 0 – 2 147 483 647 | ~3.8 MB   |
| Very large  | `sorting/very_large.bin` | 10 000 000 | 0 – 2 147 483 647 | ~38  MB   |

**Format:** `[int n][n × int]`

## Graphs

Used by BFS.

| Name        | File                     | Nodes      | Edges        | File size |
|-------------|--------------------------|------------|--------------|-----------|
| Small       | `graphs/small.bin`       | 500        | 5000         | ~39  KB   |
| Medium      | `graphs/medium.bin`      | 5 000      | 50 000       | ~391 KB   |
| Large       | `graphs/large.bin`       | 50 000     | 500 000      | ~3.8 MB   |
| Very large  | `graphs/very_large.bin`  | 500 000    | 5 000 000    | ~38  MB   |

**Format:** `[int nodes][int edges][edges × (int from, int to)]`

## Weighted Graphs

Used by Dijkstra.

| Name        | File                              | Nodes      | Edges        | Weight range | File size |
|-------------|-----------------------------------|------------|--------------|--------------|-----------|
| Small       | `graphs_weighted/small.bin`       | 500        | 5000         | 1.0 - 100.0  | ~78  KB   |
| Medium      | `graphs_weighted/medium.bin`      | 5 000      | 50 000       | 1.0 - 100.0  | ~781 KB   |
| Large       | `graphs_weighted/large.bin`       | 50 000     | 500 000      | 1.0 - 100.0  | ~7.6 MB   |
| Very large  | `graphs_weighted/very_large.bin`  | 500 000    | 5 000 000    | 1.0 - 100.0  | ~76  MB   |

**Format:** `[int nodes][int edges][edges × (int from, int to, double weight)]`

## Matrix

Used by matrix multiplication.

| Name        | File                     | Dimension  | Elements     | Value range  | File size |
|-------------|--------------------------|------------|--------------|--------------|-----------|
| Small       | `matrix/small.bin`       | 64x64      | 4 096        | 1.0 - 100.0  | ~64  KB   |
| Medium      | `matrix/medium.bin`      | 256x256    | 65 536       | 1.0 - 100.0  | ~1   MB   |
| Large       | `matrix/large.bin`       | 512x512    | 262 144      | 1.0 - 100.0  | ~4   MB   |
| Very large  | `matrix/very_large.bin`  | 1024x1024  | 1 048 576    | 1.0 - 100.0  | ~16  MB   |

**Format:** `[int n][n×n × double (matrix A)][n×n × double (matrix B)]`

# Tests

Each algorithm has a corresponding test file implemented both in C and JS, mainly for verification.
`XX` represents a binary, which can be `small`, `medium`, `large` or `very_large`.
The C test files are run with gcc, and the Javascript test files are run with node.

## Sorting
### Mergesort

```zsh
gcc -O2 -o test_mergesort mergesort.c test_mergesort.c && ./test_mergesort XX
```

```zsh
node test_mergesort.mjs XX
```

### Quicksort

```zsh
gcc -O2 -o test_quicksort quicksort.c test_quicksort.c && ./test_quicksort XX
```

```zsh
node test_quicksort.mjs XX
```

## Graphs
### BFS

```zsh
  gcc -O2 -o test_bfs bfs.c test_bfs.c && ./test_bfs XX
```

```zsh
node test_bfs.mjs XX
```

### Dijikstra

```zsh
gcc -O2 -o test_dijkstra dijkstra.c test_dijkstra.c && ./test_dijkstra XX
```

```zsh
node test_dijikstra.mjs XX
```

## Matrix
## Matrix multiplication

```zsh
gcc -O2 -o test_matrix_multiplication matrix_multiplication.c test_matrix_multiplication.c && ./test_matrix_multiplication XX
```

```zsh
node test_matrix_multiplication.mjs XX
```

# WASM

Compiling to WASM from C is done using Emscripten. 

## Mergesort

```zsh
emcc "src/c/sorting/mergesort.c" -O2 -o "src/wasm/sorting/mergesort.mjs" \
  -s EXPORTED_FUNCTIONS='["_merge_sort","_malloc","_free"]' \
  -s EXPORTED_RUNTIME_METHODS='["HEAP32"]' \
  -s MODULARIZE=1 \
  -s EXPORT_ES6=1 \
  -s EXPORT_NAME='createMergeSortModule' \
  -s ALLOW_MEMORY_GROWTH=1
```

## Quicksort

```zsh
emcc "src/c/sorting/quicksort.c" -O2 -o "src/wasm/sorting/quicksort.mjs" \
  -s EXPORTED_FUNCTIONS='["_quick_sort","_malloc","_free"]' \
  -s EXPORTED_RUNTIME_METHODS='["HEAP32"]' \
  -s MODULARIZE=1 \
  -s EXPORT_ES6=1 \
  -s EXPORT_NAME='createQuickSortModule' \
  -s ALLOW_MEMORY_GROWTH=1
```

## BFS

```zsh
emcc "src/c/graphs/bfs.c" -O2 -o "src/wasm/graphs/bfs.mjs" \
  -s EXPORTED_FUNCTIONS='["_bfs","_graph_create","_graph_build","_graph_free","_malloc","_free"]' \
  -s EXPORTED_RUNTIME_METHODS='["HEAP32"]' \
  -s MODULARIZE=1 \
  -s EXPORT_ES6=1 \
  -s EXPORT_NAME='createBFSModule' \
  -s ALLOW_MEMORY_GROWTH=1
```

## Dijkstra

```zsh
emcc "src/c/graphs/dijkstra.c" -O2 -o "src/wasm/graphs/dijkstra.mjs" \
  -s EXPORTED_FUNCTIONS='["_dijkstra","_weighted_graph_create","_weighted_graph_build","_weighted_graph_free","_malloc","_free"]' \
  -s EXPORTED_RUNTIME_METHODS='["HEAP32","HEAPF64"]' \
  -s MODULARIZE=1 \
  -s EXPORT_ES6=1 \
  -s EXPORT_NAME='createDijkstraModule' \
  -s ALLOW_MEMORY_GROWTH=1
```

## Matrix multiplication

```zsh
emcc "src/c/numeric/matrix_multiplication.c" -O2 -o "src/wasm/numeric/matrix_multiplication.mjs" \
  -s EXPORTED_FUNCTIONS='["_matrix_multiplication","_malloc","_free"]' \
  -s EXPORTED_RUNTIME_METHODS='["HEAPF64"]' \
  -s MODULARIZE=1 \
  -s EXPORT_ES6=1 \
  -s EXPORT_NAME='createMatrixModule' \
  -s ALLOW_MEMORY_GROWTH=1
```

